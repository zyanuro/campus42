
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

 /*size_t ft_strlcat(char *dst, const char *src, size_t dstsize)
 {
    return dstsize;
 }

int main() {
    // Declaración de variables
    char destino[20] = "Hola ";
    char origen[] = "mundo!";
    
    // Obtener el tamaño del búfer de destino
    size_t longitud_destino = sizeof(destino);
    // Obtener la longitud actual de la cadena en destino
    size_t longitud_actual = strlen(destino);
    
    // Concatenar la cadena origen en la cadena destino de manera segura
    size_t resultado = strlcat(destino, origen, longitud_destino - longitud_actual);
    
    // Imprimir el resultado
    printf("Cadena destino: %s\n", destino);
    printf("Longitud de la cadena concatenada: %zu\n", resultado);

    return 0;
} */